# Handoff → Sol

**Continue this task where Opus left off. Read this whole file first, then read the calibration + memory files listed under "Voice rules" before touching any prose.**

---

## The task

Two things to the **pg_stats blog post** (`collections/_posts/2026-07-18-the-130x-lie-in-pg-stats.md`), both approved by the user:

1. **Voice-align it** to the first-person war-story register of the cache-aside post (`collections/_posts/2026-07-18-cache-aside-is-four-lines.md`). Right now pg_stats is in a *clinical* register and reads as the odd one out in the series.
2. **Add a reproducible local Postgres benchmark** and **replace the production numbers with locally-measured ones**, with themeable CSS/SVG charts, and commit the harness.

This is the same play we just did for the cache-aside post (post + `benchmarks/cache-aside/`), applied to pg_stats. Mirror that structure.

## Decisions already locked (user answered — do NOT re-ask)

1. **Scope: benchmarks + voice align.** Both. (This time it's approved and calibrated — a previous agent did a *silent, unrequested* voice rewrite of this exact post and it was reverted. The revert was about it being silent, not about the direction. You have the green light.)
2. **Replace prod numbers with local numbers.** Every measured number, EXPLAIN plan, `n_distinct` value, and timing in the post must come from YOUR local run and be presented as local. Do not keep the 500M-row / 1.9 TB / 17.4M-rows / 130x / 50,000x production figures — those are being replaced.
3. **Local scale: ~5M rows** (bump toward 20M only if the bad plan won't reproduce at 5M — see the landmine below).
4. **Charts: themeable inline CSS/SVG**, dark-mode aware. **Reuse the exact `<style>` block + `.cache-bench` classes from the cache-aside post** so the two posts look like one series. Don't invent a new palette.

## ⚠️ Two landmines specific to this post — read carefully

**A. The title contains a production number.** The post is titled *"The 130x Lie in pg_stats"*. The 130x is the production `n_distinct` miss ratio (real 5M distinct vs ANALYZE's 38,500). At 5M rows your local miss ratio will be different. So: **measure your local ratio, then retitle with the real local number** (e.g. "The 40x Lie in pg_stats"), or generalise the title if the number is unstable across runs. Do NOT leave "130x" in the title if your local run doesn't produce 130x — that would be a fabricated number sitting in the headline. Update the filename slug too only if you must; prefer keeping the slug and changing just the `title:` (the post is already committed/pushed under this slug, and the cache-aside post links nowhere to it, but a slug change changes the URL).

**B. The bad plan might not reproduce at 5M.** The whole story depends on the planner picking the primary-key scan + filter (the LIMIT-trap) instead of the `session_id` index. That only happens if ANALYZE underestimates `n_distinct` badly enough. At 5M it *should*, but it is NOT guaranteed. **You must verify the trap actually fires** (EXPLAIN shows the pkey scan with a big `Rows Removed by Filter`). If it doesn't:
   - First bump scale toward 20M rows.
   - Then worsen the underestimate naturally — higher null fraction, more events per session (tighter clustering).
   - Only as a last resort touch planner cost knobs, and if you do, **disclose it in the post**.
   - **Never hand-write or doctor an EXPLAIN plan to make the story work.** If the trap genuinely won't reproduce locally, STOP and tell the user rather than faking it.

## Voice rules — the user rejected AI-tone repeatedly this session

Read before writing ANY prose:
- Calibration exemplar (the target voice): `collections/_posts/2026-07-18-cache-aside-is-four-lines.md`
- Calibration exemplar: `interview/low-level-design/lld-framework.md`
- Feedback memory (READ IT — full rules): `~/.claude/projects/-Users-akisonlyforu-Code-akisonlyforu-github-io/memory/feedback_writing_style.md`

Summary:
- Raw first-person war-story. Talk to the reader as a peer. Comma-splice run-ons over balanced clauses.
- **Self-deprecation is about vanity ONLY, never competence.** Frame the slowdown as *latent-until-scale* ("the query was fine for months, then…"), never as a rookie mistake.
- **Replace the clinical headers** — "Problem Statement", "The Data", "The Query", "Root Cause Analysis", "1. Look at the plan", "Solution", "Operational Notes" — with casual/personal ones like the cache-aside post uses ("Following the plan", "The lie in pg_stats", "The fix", "Stuff worth remembering" are fine directions; make them yours).
- **Never generate these AI tells:** aphoristic antithesis "X is a Y, not a Z"; formulaic parallel headers; tidy wrap-up headers like "The honest summary"; over-hedged qualifiers; spec-sheet-terse table cells.
- **Keep 100% of the technical substance:** the schema, the LIMIT-discount mechanism, the `rows ≈ total × (1−null_frac) / n_distinct` formula, the Haas–Stokes + clustering explanation, the SET STATISTICS fix, and the operational caveats (cost of higher stats target, reset on `pg_upgrade`, not-Postgres-specific). Only the *numbers* and the *voice* change.

## Honesty framing (since numbers are now local)

Because we're replacing prod numbers with local ones, the post can no longer claim a 500M-row production incident. Reframe honestly — the audit-trail scenario (an `audit_events` table with a sparse, clustered `session_id`) is a legitimate real-world *shape* and can stay as the setup, but the demonstrated numbers are from a table you built and ran locally, and the post should read that way ("here it is, small enough that you can run it yourself"). Add a short in-voice note that these are laptop numbers at ~5M rows — the mechanism is what transfers, not the absolute timings — and link to the committed harness. Do NOT blur local and prod.

## Benchmark design

Mirror `benchmarks/cache-aside/` (docker-compose + schema.sql + a Python or SQL harness + README + results/ CSVs + run_metadata.csv). Put it under `benchmarks/pg-stats/`.

- **Postgres 16 in Docker** (reuse the compose pattern; ports 5432 currently free but map to something like 55432 to dodge the user's other stacks). No Redis needed for this one (user has it, but this is a pure-Postgres story).
- **Schema:** `audit_events` (`id BIGSERIAL PK, entity_type, entity_id, session_id UUID, event_type, payload JSONB, created_at`) + `admin_sessions` (`id UUID PK, admin_email, ip_address`) + `CREATE INDEX idx_audit_events_session_id ON audit_events(session_id)`. Same as the post.
- **Seed ~5M rows** preserving the three knobs that create the bug: ~82% NULL `session_id`; non-null sessions in **clustered bursts** (~18 events per session, inserted contiguously so one session's rows share physical pages — the table is append-only ordered by insert). That gives ~50K distinct non-null sessions at 5M rows (real `n_distinct` ≈ 50K — measure the exact value).
- **Measure, at `default_statistics_target` = 100, then SET STATISTICS 2000, then 5000 (ANALYZE ×2):**
  - `n_distinct` and `null_frac` from `pg_stats` at each target (the underestimate → improvement).
  - The `LIMIT 1` join query under `EXPLAIN (ANALYZE, BUFFERS)`: plan chosen, `Rows Removed by Filter`, buffer reads, execution time — before (trap) and after (index scan).
  - Planner's estimated rows vs actual matching rows (the local analog of "2,670 vs 18").
  - Compute the real miss ratio = real `n_distinct` / ANALYZE-estimated `n_distinct` → this is your new title number.
- **Charts (themeable CSS/SVG, reuse `.cache-bench`):**
  - `n_distinct` estimate vs statistics target (100/2000/5000) with a reference marker for the real value.
  - Query latency before vs after (bar).
  - Planner estimated-rows vs actual-rows (bar) — the LIMIT-trap in one picture.
- **Results → CSV** in `benchmarks/pg-stats/results/`, plus a `run_metadata.csv` (timestamp, platform, pg image, seed rows, statistics targets, measured real n_distinct). Real timestamps are fine here — you're running real shell commands, not a Workflow script.

## What's already done (context)

- Cache-aside post + `benchmarks/cache-aside/` are **committed and pushed** (commit `215487c`, on `master`). Don't touch them; use them as your template.
- pg_stats post is currently **reverted to its original committed version** (clean) — you're starting from the clinical original.
- Jekyll dev server was running in background (livereload) at `http://127.0.0.1:4000/`, task id `b9yraygrr`. Post URL after your edits: `http://127.0.0.1:4000/blog/the-130x-lie-in-pg-stats/` (permalink is `/blog/:title/` → the slug from the filename, `the-130x-lie-in-pg-stats`; changing the filename changes the URL). Restart the server if it's died.
- Env verified earlier: Docker 28.3.2, compose v2.38.2, python 3.9.6, ports 5432/6379 free.

## Verify before you call it done (mirror how the cache-aside post was checked)

- Every number in the post traces to a row in your committed CSVs.
- The EXPLAIN plans in the post are **real output you captured**, not edited.
- The bad plan genuinely reproduced (landmine B) — or you stopped and flagged it.
- Post builds and serves HTTP 200; the 3 chart figures render; raw HTML isn't escaped by kramdown; both `prefers-color-scheme` and `data-theme="dark"` rules are present.
- Voice: no clinical headers left, no AI tells, first-person throughout, technical substance intact.

## Git / repo state — do NOT commit or push

- **Leave everything uncommitted for the user to review.** This is a rewrite of a *published* post; the user reviews the voice change before it goes live. (Unlike the cache-aside post, which the user explicitly told us to push.)
- Do not touch: `interview/low-level-design/index.md` (user's own uncommitted work), `_to_delete/`, `.handoff-sonnet.md`, `.handoff-sol.md`.
- Branch: `master`.

## Gotchas

- Seeding 5M clustered rows: generate in insertion order so each session's rows are contiguous (e.g. build sessions one at a time, or sort by session before bulk `COPY`). Random insertion destroys the clustering and the bug won't reproduce.
- `n_distinct` in `pg_stats` can be negative (a ratio) for some columns — handle both the absolute and the `-0.x × reltuples` forms when you read it.
- Run `ANALYZE` after seeding and after every `SET STATISTICS` change, or you're reading stale stats.
- If you change the post's `title:` to a new ratio, decide deliberately whether to also rename the file (URL change) — default to keeping the filename/slug and only changing the `title:` front-matter.
