# benchmark.py uses only the Python standard library (csv, os, platform, re,
# statistics, subprocess, sys, time, datetime). No third-party packages are
# required. This file is kept for parity with the other harnesses in benchmarks/.
