# No third-party dependencies. benchmark.py is stdlib-only
# (logging, logging.handlers, queue, json, csv, statistics, time, tempfile, random).
# This file exists to match the harness convention; nothing to install.
